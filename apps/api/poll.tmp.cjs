const { PrismaService } = require("@seeding/database"); async function main() { const p = new PrismaService(); try { const j = await p.processingJob.findUnique({ where: { id: "cmsbtj7610005k55kgbix1ydj" }, select: { status: true, progress: true, processedItems: true, failedItems: true } }); console.log("job:", JSON.stringify(j)); } finally { await p.$disconnect(); } } main().catch((e) => { console.error("ERR", e.message); process.exit(1); });