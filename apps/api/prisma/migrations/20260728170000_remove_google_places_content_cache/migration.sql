-- Google Places Content (ngoài Place ID) không được lưu trong cache nghiệp vụ.
-- Bảng này chỉ chứa preview ngắn hạn và không phải dữ liệu do người dùng sở hữu.
DROP TABLE IF EXISTS "google_place_preview_cache";
