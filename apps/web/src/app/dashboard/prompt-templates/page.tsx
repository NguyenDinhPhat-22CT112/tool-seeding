import { PromptTemplateManager } from "@/components/contents/prompt-template-manager";

export const metadata = {
  title: "Prompt Template AI",
};

export default function PromptTemplatesPage() {
  return <PromptTemplateManager />;
}
